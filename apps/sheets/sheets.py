def append_row(values: list):
    """
    Stub for Google Sheets integration.
    Later this will call the Sheets API; for now we just log.
    """
    print(f"[sheets] append_row called with: {values}")
